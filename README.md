# django-parrallel-sessions

## Usage
```shell
pip install -e .
```
```shell
dps --port=<port> --branch=<branch> --venv=<path_to_venv> \
    --dst=<path_to_destionation_folder> --django=<path_to_django_app> --host=<django_host>
```